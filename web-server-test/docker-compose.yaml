services:
  web:
    image: ghcr.io/jcsimon1/web-server-test:latest
    restart: unless-stopped
    container_name: compose-web
    ports:
      - "${PORT_EXTERNAL:-8080}:80"
    volumes:
      - ${SOURCE_LOCATION}:/usr/share/nginx/html:ro