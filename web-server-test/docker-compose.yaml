services:
  web:
    image: ghcr.io/jcsimon1/web-server-test:latest
    restart: unless-stopped
    container_name: compose-web
    ports:
      - "7777:80"
    volumes:
      - /mnt/smbshare/tmp/web/html:/usr/share/nginx/html:ro